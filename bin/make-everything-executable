#!/usr/bin/env bash

set -eou pipefail

root_dir=$(pwd)

main_scripts="$root_dir/*.sh"
exercise_scripts="$root_dir/exercises/**/*.sh"
kata_scripts="$root_dir/katas"

scripts="$main_scripts $exercise_scripts $kata_scripts"

for script in $scripts
do
  chmod +x "$script"
done
